mutation Register($email: String!, $password: String!){
    register(registerInput: { email: $email, password: $password}){ 
        errors {
            field
            message
        },
        user {
            email,
            updatedAt,
            createdAt
        }
    }
}
