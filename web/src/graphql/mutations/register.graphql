mutation Register($userInput: UserInput!){
    register(registerInput: $userInput){ 
        errors {
            field
            message
        },
        user {
            ...RegularUser
        }
    }
}
